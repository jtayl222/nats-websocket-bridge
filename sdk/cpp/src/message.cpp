/**
 * @file message.cpp
 * @brief Message utility implementations
 */

#include "gateway/message.h"

namespace gateway {

// JsonValue and Message implementations are primarily inline in the header,
// but we can add any complex operations here if needed.

} // namespace gateway
